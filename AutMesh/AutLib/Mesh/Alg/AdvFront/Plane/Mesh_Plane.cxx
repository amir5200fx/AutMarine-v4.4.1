#include <Mesh_Plane.hxx>

namespace AutLib
{
	const Standard_Integer Mesh_PlaneInfo::ALLOWED_MAX_LEVEL_GENERATION(1500);

	const Standard_Real Mesh_PlaneInfo::DEFAULT_LOCALFRONT_FACTOR(1.05);
	const Standard_Real Mesh_PlaneInfo::DEFAULT_MIN_DISTANCE_FACTOR(0.2);
}